# Site

English content hub built with Astro, hosted on Cloudflare Pages.

```bash
npm ci
npm run dev      # local dev server
npm run build    # outputs to dist/
```

Cloudflare Pages settings: root directory `site`, build command `npm run build`, output directory `dist`.
