# Examples

- `requests/` — HTTP request files for exploring the API (VS Code REST Client / IntelliJ HTTP client compatible)
- `sample-documents/` — sample documents for the RAG indexing pipeline (added with Day 12)
